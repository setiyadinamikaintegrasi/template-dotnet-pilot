# Branching Strategy

**Status:** Template policy.

Trunk-based: short-lived branches off `main`, merged via PR. No direct push to `main`. Squash-merge by default with a Conventional Commit PR title. See `CONTRIBUTING.md`.
