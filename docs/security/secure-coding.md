# Secure Coding

**Status:** Adapt to your project.

- Validate all input; parameterize queries; encode output.
- Never trust LLM output — validate against a schema before use.
- No secrets in logs; no debug mode in production.
- Follow `AGENTS.md` security rules; pass CodeQL/Semgrep before merge.
