# Data Flow

**Status:** Adapt to your project.

Document the primary data flows, including where sensitive data moves and where authorization is enforced. For RAG: ingestion → parsing → chunking → embedding → indexing → retrieval → authorization filter → model → response.
