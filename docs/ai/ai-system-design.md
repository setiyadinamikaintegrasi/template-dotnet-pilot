# AI System Design

**Status:** Adapt to your project.

High-level design of the AI subsystem: model abstraction layer, prompt management, structured output, evaluation, observability. Business logic must not call a provider SDK directly — use a controlled adapter/gateway (see [model-routing.md](model-routing.md)).
