# Tool Permissions

**Status:** Adapt to your project.

Per-tool allow/deny matrix. High-impact tools require explicit human approval. Never grant unrestricted access to shell, production DB, cloud console, email, source-control admin, infrastructure, payments, production deployment, or secrets.
