# Model Routing

**Status:** Adapt to your project.

How requests are routed across providers/models, including fallback, timeout, retry, rate limits, circuit breakers, and cost/token caps. Implemented in the adapter/gateway layer, never in business logic.
