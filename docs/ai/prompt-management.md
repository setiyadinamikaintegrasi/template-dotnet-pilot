# Prompt Management

**Status:** Adapt to your project.

Production prompts live in [../../prompts/registry.yaml](../../prompts/registry.yaml). Each prompt has: id, name, purpose, version, owner, prompt reference, system prompt reference, input, output schema, model compatibility, safety constraints, eval dataset, changelog, deprecation status. Prompt changes are reviewed like code and trigger AI evaluations.
