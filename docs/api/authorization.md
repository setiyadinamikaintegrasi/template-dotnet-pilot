# Authorization

**Status:** Adapt to your project.

Describe the authorization model: roles, scopes, tenant isolation, row-level/resource-level checks. Authorization must be enforced on every request and **before** retrieved content is supplied to a model (RAG).
