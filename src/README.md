# src/

**Consumer-owned.** The template is stack-agnostic — no language or framework is committed here. Adopt your stack, then wire the Makefile targets (`scripts/detect-stack.sh` detects it automatically) and start filling `tests/`.

See [../AGENTS.md](../AGENTS.md) for boundaries and [../docs/development/local-setup.md](../docs/development/local-setup.md) to begin.
